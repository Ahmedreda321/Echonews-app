import 'package:flutter/material.dart';
import 'package:flutter_screenutil/flutter_screenutil.dart';

class AppPadding {
  //
  AppPadding._();
  static EdgeInsets appBarPadding = EdgeInsets.only(top: 16.0.h);

  static EdgeInsets categoryPadding = EdgeInsets.all( 6.w);

  static EdgeInsets newsPadding = EdgeInsets.all(18.w);
}
